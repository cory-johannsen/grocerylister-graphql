export default class Store {
  constructor(id, name, groceryListId, departments) {
    this.id = id
    this.name = name
    this.groceryListId = groceryListId
    this.departments = departments
  }
}
